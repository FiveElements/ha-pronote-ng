# Home Assistant pins lru-dict==1.3.0, which publishes no wheel for CPython
# 3.13; 1.4.1 does and is API-compatible. Without this, a local install needs a
# C toolchain on Windows for no benefit.
lru-dict>=1.4.1
